plugins {
    id "java"
}

java {
    archivesBaseName = "${rootProject.name}-${project.name}"
    toolchain.languageVersion = JavaLanguageVersion.of(8)
}

jar {
    // Exclude the fake @Mod annotation class. Obviously.
    exclude('net')
}
